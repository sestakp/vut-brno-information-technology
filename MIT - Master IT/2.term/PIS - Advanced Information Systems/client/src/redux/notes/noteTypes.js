import getBaseTypes from "../base/TypeBase";

let noteTypes = getBaseTypes("NOTE");

noteTypes.SET_TEXT_NEW_NOTE = "SET_TEXT_NEW_NOTE";
noteTypes.NOTE_RESET = "NOTE_RESET";

export default noteTypes;
